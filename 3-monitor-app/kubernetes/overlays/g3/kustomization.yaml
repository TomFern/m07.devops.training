apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: g3

patches:
  - target:
      kind: Service
      name: grafana
    patch: |
      - op: replace
        path: /spec/ports/0/nodePort
        value: 30043